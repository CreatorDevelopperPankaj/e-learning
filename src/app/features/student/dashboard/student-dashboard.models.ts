export type StudentPopularCourse = {
  id: string;
  title: string;
  shortDescription: string;
  thumbnail: string;
  rating: number;
  totalStudents: number;
  duration: string;
  currency: string;
  discountPrice: number;
  price: number;
  instructorName: string;
  slug?: string;
};

